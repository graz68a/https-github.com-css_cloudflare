# CSS Cloudflare Worker

This Cloudflare Worker injects critical CSS into your WordPress blog pages using `HTMLRewriter`.

## Features
- Injects CSS for Home and Post pages.
- Optimizes page load by providing critical CSS inline.
- Easy to deploy and configure.

## Usage
Deploy this script to your Cloudflare Worker and route it to your WordPress domain.
